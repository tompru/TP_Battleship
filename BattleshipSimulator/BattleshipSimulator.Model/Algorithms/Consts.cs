namespace BattleshipSimulator.Model.Algorithms
{
    internal static class Consts
    {
        internal static readonly List<Direction> Directions = new()
        {
            Direction.Left,
            Direction.Right,
            Direction.Up,
            Direction.Down
        };
    }
}
